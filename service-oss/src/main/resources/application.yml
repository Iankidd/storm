server:
  port: 8762
  servlet:
    context-path: /storm_oss

spring:
  application:
    name: service-oss
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/storm_oss
    username: root
    password: 123456aB
    driver-class-name: com.mysql.jdbc.Driver
  profiles:
    active: dev
  thymeleaf:
    mode: HTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false

mybatis:
  mapper-locations: classpath:mapper/*.xml

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/